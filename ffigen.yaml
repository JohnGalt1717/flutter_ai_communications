# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterAiCommunicationsBindings
description: |
  Bindings for `src/flutter_ai_communications.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_ai_communications_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_ai_communications.h'
  include-directives:
    - 'src/flutter_ai_communications.h'
ffi-native:
comments:
  style: any
  length: full
