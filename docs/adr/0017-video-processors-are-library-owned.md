# Video processors are library-owned and selectable

Person isolation is part of the package, not a host GPU experiment. The Session and Pre-join preview select one Video processor: none, blur with intensity, or replace with host-supplied background bytes or an asset path. The type defines the behavior. Processors run on the Production video path so preview matches the call. Hosts do not inject a second processor object until a second processor family exists.
