# Pre-join preview is idle manager state, not a Session

Superseded by ADR-0020. Lobby is a Session with diagnostic attachments and no Transport plugin. In-call camera settings use Camera preview, not a second Session and not idle-manager Texture state.
