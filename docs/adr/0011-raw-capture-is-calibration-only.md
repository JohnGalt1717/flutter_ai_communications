# Raw capture is calibration-only

The library does not expose a second public raw capture stream because it would let hosts diverge recording, visualization, and Transport behavior. Debug builds may produce short, bounded, explicitly consented pre-processor snapshots for Acoustic-profile calibration and physical-device evidence only.
